<?php
$names = ["Carol", "Andy", "Betty", "Daniel"];

// sort($names); // アルファベット順に
// $reversed = array_reverse($names); // 順番を逆に
// var_dump($reversed);

rsort($names); // reverse sort、逆アルファベット順
var_dump($names);

